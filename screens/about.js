import { Text, View,  } from "react-native";
import { globalStyles } from "../globalStyles";

export default function About({navigator}) {

    return (
        <View style={globalStyles.container}>
            <Text>
                Home Screen
            </Text>
        </View>
    )
}